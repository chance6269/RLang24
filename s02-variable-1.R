# 변수와 자료형
# 변수이름 작성 규칙
# - 첫자는 영문자로 시작
# - 대소문자 구분
# - 특수문자: (_), (.)
# - 한 번 선언된 변수는 새로운 자료형으로 변경 가능
# - 카멜표기법 : 여러 단어를 결합할 때 
#   첫 번째 단어는 소문자
#   두 번째 단어는 대문자로 시작

# 할당 : =, <-
# 변수
a = 10
b = 20
c = a + b

d <- 10
e <- 20
f <- d + e

rm(a)
